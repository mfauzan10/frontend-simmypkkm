export default interface ArticleInterface {
  _id: string;
  category: string;
  categoryId: string;
  createdAt: string;
  featuredImage: string;
  summary: string;
  title: string;
  view: number;
}
