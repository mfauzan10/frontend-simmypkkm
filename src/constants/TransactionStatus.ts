// Status kepegawaian
export enum TransactionStatus {
  Inactive = 'inactive',
  Pending = 'pending',
  Settlement = 'settlement',
  Capture = 'capture',
  Deny = 'deny',
  Cancel = 'cancel',
  Expire = 'expire',
  Refund = 'refund',
}

export default TransactionStatus
